#def followplayer(dot, options):
  #options = [
               # vector(5, 0),
                #vector(-5, 0),
                #vector(0, 5),
                #vector(0, -5),
           # ]
  #up()
  #goto(point.x + 10, point.x + 10)
  #dot(20,"pink")
